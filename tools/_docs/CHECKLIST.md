# Tools CHECKLIST

- [ ] Confirm OS/shell availability (PowerShell/Pwsh, Bash, Python).
- [ ] Set required environment variables (see individual tool docs).
- [ ] Run from repo root (unless tool doc says otherwise).
- [ ] Dry-run flags? Prefer `-WhatIf`/`-Verbose` where available.
- [ ] Capture logs/artifacts to `artifacts/tools/<tool>/<timestamp>/`.
- [ ] Update RTM or Status Report if tool modifies test artifacts.
